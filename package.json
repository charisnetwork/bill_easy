{
  "name": "mybillbook-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "node railway-monorepo.js",
    "install-all": "npm install --force --legacy-peer-deps --prefix backend && npm install --force --legacy-peer-deps --prefix admin/backend",
    "postinstall": "npm run install-all"
  },
  "dependencies": {
    "express": "^4.21.0",
    "http-proxy-middleware": "^3.0.0",
    "@google/generative-ai": "^0.24.1"
  }
}
