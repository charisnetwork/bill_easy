{
  "name": "bill-easy-admin-gateway",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd backend && npm install --omit=dev && cd ../frontend && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "http-proxy-middleware": "^3.0.3"
  }
}
