/**
 * Root server.js for Railway/Hosting compatibility.
 * This launches the monorepo gateway which manages both Main and Admin backends.
 */
console.log('🚀 Launching Bill Easy Monorepo Gateway from root server.js...');
require('./railway-monorepo.js');
